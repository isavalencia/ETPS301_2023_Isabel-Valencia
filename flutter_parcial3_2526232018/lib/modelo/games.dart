import 'package:flutter/material.dart';

class Games {
  String title;
  String game_url;

  Games(
    this.title,
    this.game_url,
  ) {
    this.title = title;
    this.game_url = game_url;
  }
}
